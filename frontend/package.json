{
  "name": "draco-tauri-frontend-contracts",
  "private": true,
  "version": "2.0.3",
  "type": "module",
  "scripts": {
    "check": "node --check dist/app.js && node --check dist/result-export.js",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "qrcode": "^1.5.4"
  }
}
