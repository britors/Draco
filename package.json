{
  "name": "draco",
  "productName": "Draco",
  "description": "Multi-database explorer and query runner",
  "version": "11.1.4",
  "author": {
    "name": "britors",
    "email": "rodrigo@w3ti.com.br"
  },
  "license": "GPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/britors/Draco.git"
  },
  "main": "out/main/index.js",
  "scripts": {
    "dev": "node esbuild.js && electron out/main/index.js",
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "package": "node esbuild.js --production && electron-builder --publish never",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/__tests__/PgConnection.test.ts src/__tests__/DracoParser.test.ts src/__tests__/ConnectionManager.test.ts",
    "flatpak:gen-sources": "flatpak-node-generator npm package-lock.json -o flatpak/generated-sources.json",
    "flatpak:build": "flatpak-builder --force-clean build-dir flatpak/br.com.w3ti.Draco.yml",
    "flatpak:run": "flatpak-builder --force-clean --run build-dir flatpak/br.com.w3ti.Draco.yml run.sh"
  },
  "build": {
    "appId": "com.britors.draco",
    "productName": "Draco",
    "directories": {
      "output": "dist"
    },
    "files": [
      "out/**/*"
    ],
    "npmRebuild": false,
    "linux": {
      "target": [
        "deb",
        "rpm"
      ],
      "icon": "logo.svg",
      "artifactName": "${name}_${version}_amd64.${ext}"
    },
    "win": {
      "target": "nsis",
      "icon": "logo.ico",
      "artifactName": "${productName}Setup_${version}.${ext}"
    }
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@types/pg": "^8.11.0",
    "@types/ssh2": "^1.15.5",
    "@vscode/codicons": "^0.0.45",
    "electron": "^42.4.0",
    "electron-builder": "^26.8.1",
    "esbuild": "^0.28.0",
    "playwright-core": "^1.60.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "pg": "^8.11.0",
    "ssh2": "^1.17.0"
  }
}
